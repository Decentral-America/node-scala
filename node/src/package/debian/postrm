${{header}}

if [ "$1" = purge ]; then
    rm -rf /var/lib/${{app_name}}/data
    rm -rf /var/log/${{app_name}}

    userdel dcc >/dev/null 2>&1 || true
    groupdel dcc >/dev/null 2>&1 || true
fi

exit 0
