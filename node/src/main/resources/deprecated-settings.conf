dcc {
  db {
    directory = ${?dcc.data-directory}
    max-rollback-depth = ${?dcc.max-rollback-depth}
    max-cache-size = ${?dcc.max-cache-size}
    remember-blocks = ${?dcc.remember-blocks-interval-in-cache}
  }
}

pekko.http.server {
  idle-timeout = ${?akka.http.server.idle-timeout}
  request-timeout = ${?akka.http.server.request-timeout}
}
