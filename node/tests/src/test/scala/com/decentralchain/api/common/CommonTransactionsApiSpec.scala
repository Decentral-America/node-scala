package com.decentralchain.api.common

import com.decentralchain.test.FreeSpec

class CommonTransactionsApiSpec extends FreeSpec {
  "Merkle Tree" - {
    "finds proofs for transactions from supported block versions" in pending
    "excludes transactions from older block versions" in pending
  }
}
